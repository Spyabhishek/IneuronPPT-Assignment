Q.1 <!DOCTYPE html> is it a tag of html? If not, what is it and why do we use it?

Ans. - <!DOCTYPE html> is not a HTML tag. It is a declaration that means document type. It help browsers interpret and render the content correctly. 